{
  "rules": {
    "rooms": {
      "$roomCode": {
        ".read": true,
        ".write": true,

        ".validate": "newData.hasChildren(['players'])"
      }
    },

    "$other": {
      ".read": false,
      ".write": false
    }
  }
}
